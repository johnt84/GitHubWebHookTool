name: Build and deploy .NET Core Function App to Azure functions app
on:
  push:
    branches:
    - master
    paths:
    - 'GitHubWebHookTool/**'
env:
  AZURE_FUNCTIONAPP_NAME: GitHubWebHookTool
  AZURE_FUNCTIONAPP_PACKAGE_PATH: GitHubWebHookTool/publish
  CONFIGURATION: Release
  DOTNET_CORE_VERSION: 6.0.x
  WORKING_DIRECTORY: GitHubWebHookTool
  TEST_DIRECTORY: GitHubWebHookToolUnitTests
jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}
    - name: Restore
      run: dotnet restore "${{ env.WORKING_DIRECTORY }}"
    - name: Build
      run: dotnet build "${{ env.WORKING_DIRECTORY }}" --configuration ${{ env.CONFIGURATION }} --no-restore
    - name: Publish
      run: dotnet publish "${{ env.WORKING_DIRECTORY }}" --configuration ${{ env.CONFIGURATION }} --no-build --output "${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}"
    - name: Test
      run: dotnet test "${{ env.TEST_DIRECTORY }}"
    - name: Deploy to Azure Function App
      uses: Azure/functions-action@v1
      with:
        app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
        publish-profile: ${{ secrets.GitHubWebHookTool_ec91 }}
        package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
